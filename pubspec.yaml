name: football
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.10.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.4.3
  flutter_secure_storage: ^9.2.2

  # ✅ استخدم Riverpod stable v2
  flutter_riverpod: ^2.5.1
  riverpod: ^2.5.1

  # ✅ Models + Json
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  go_router: ^17.1.0
  google_fonts: ^8.0.2
  cached_network_image: ^3.4.1
  http: ^1.6.0
  image_picker: ^1.2.1
  mobile_scanner: ^7.2.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

  build_runner: ^2.5.4
  freezed: ^2.5.8
  json_serializable: ^6.9.5

flutter:
  uses-material-design: true